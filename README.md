# NumberGeneratorWithKafka

1. Go to the "KafkaDockerFiles" folder.
2. Open Windows PowerShell inside the folder and type the follwing command: docker-compose -f docker-compose-single-broker.yml up -d.
